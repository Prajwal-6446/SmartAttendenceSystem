@page
@model Biometric_Attendence1.Pages.MDelete1Model
@if (TempData["Success"] != null)
{
	<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
	<script src="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/js/toastr.min.js"></script>
	<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/css/toastr.min.css" />
	<script type="text/javascript">
		toastr.success('@TempData["Success"]');
	</script>
}


<form method="post">
	<input hidden asp-for="ManageUser.Id" />
	<div class="border p-3 mt-4">
		<div class="row pb-2 text-center form-algerian">
           <div class="col">
                <h2>Delete User</h2>
                <hr class="w-25 mx-auto">
           </div>
         </div>

		<div class="mb-3">
			<label asp-for="ManageUser.Name"></label>
			<input asp-for="ManageUser.Name" disabled class="form-control" />
		</div>
		<div class="mb-3">
			<label asp-for="ManageUser.FingerId"></label>
			<input asp-for="ManageUser.FingerId" disabled class="form-control" />
		</div>

        <div class="btn-group">
            <div class="btn-item">
                <button type="submit" id="Delete"  class="btn btn-primary btn-contain btn-link text-line" style="width:150px;">Create</button>
            </div>
            <div class="btn-item">
                <a asp-page="MIndex1" class="btn btn-secondary btn-contain text-line" style="width:150px;">Back to List</a>
            </div>
        </div
	</div>
</form>

<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script>
    $(document).ready(function () {

        $("#Delete").on("click", function (e) {
            e.preventDefault(); // Prevent the default form submission
            debugger;
            var fingerId = $("#fingerId").val();

            // Make an AJAX POST request to the NodeMCU server to capture the fingerprint
            $.ajax({
                url: 'http://192.168.0.198/deleteFingerprint', // Replace with your NodeMCU IP address and endpoint
                type: 'POST',
                data: { fingerId: fingerId }, // Send the finger ID to the server
                success: function (response) {
                    debugger;
                    if (response == "-1") {
                        alert('error capturing fingerprint')

                    }
                    else {
                        alert('Fingerprint captured successfully', response.fingerprintID);
                        console.log(response)
                        SendDataToServer(response, fingerId, name, gender, date)
                    }
                },
                error: function (xhr, status, error) {
                    console.error('Error capturing fingerprint:', error); // Log any errors
                    // Handle errors, e.g., display an error message
                }
            });
        });

    });
   </script>
<script src="~/js/Background.js"></script>

